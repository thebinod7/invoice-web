import React from 'react';
import SubscriptionClient from './SubscriptionClient';

export const metadata = {
  title: 'Subscription - Invomaker',
};

export default function page() {
  return <SubscriptionClient />;
}
