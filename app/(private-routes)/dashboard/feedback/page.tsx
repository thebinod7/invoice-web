import React from 'react';
import FeedbackClient from './FeedbackClient';

export const metadata = {
  title: 'Feedback - Invomaker',
};

export default function page() {
  return <FeedbackClient />;
}
